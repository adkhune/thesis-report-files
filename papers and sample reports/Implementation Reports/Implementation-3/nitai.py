import numpy as np

print( np.array([10*np.random.random_sample(), 0.0]) )
#print(10*np.random.random_sample())
print(np.array([np.random.randint(1,10), 0.0]))
print(np.random.randint(-1,1)) #produce a random int between 1-9

initialState = 1
process = 0.0
#return np.array([np.random.randint(1,10), 0.0])
print(np.array([initialState, process]))
